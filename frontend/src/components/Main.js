import React from 'react';
import Header from './Header';

var Main = React.createClass({
    render: function () {
        return (
            <div className="container">
                <Header />
            </div>
        );
    }
});


export default Main;